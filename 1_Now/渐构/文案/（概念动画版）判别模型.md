> 出处：Modevol《B站用户文案（非卖品）》（《渐构世界模型》配套文案）· 作者 YJango
> 原文：https://www.modevol.com/episode/t3jpoee9bglvswysb48jri5m
> 格式说明：蓝=描述 · 红=例子 · 黄=类比/思想实验 · 白=带标签卡片（提问/目标/策略/要求/注释/格式/过渡）

## 判别与联结

> [!question] 提问：如何把握外延
>
> 根据上一节内容，我们得出了「学习的第一原则」，即"明确知识的输入空间和输出空间"，简称"明确输入输出"。
>
> 关于"具体如何明确"，在不同类型的学习中，如「继承学习」「创验学习」「外显学习」和「内隐学习」中，会有一些差异。例如，在继承他人知识时，我们还需要进行「从语言世界到概念世界的转换」，排除语言中的干扰内容，提取出输入和输出。这些细节会在不同的学习类型中进一步展开。然而，无论哪种学习类型，「明确输入输出」都必然涉及到「把握概念的外延」。这是因为「空间」对应着「概念的外延」，所以"明确输入空间和输出空间"也就等同于"明确输入和输出中每个概念的外延"。
>
> 不过，这里出现了一个问题。我们「建构知识」就是为了「对应未见情况」，这意味着我们在「建构（学习）知识」时，无法见到「外延中的所有情况」，那要如何把握「没见过的东西」呢？
>
> 例如，「光的反射定律」的「输入变量」是「入射光线、反射面」，「输出变量」是「反射光线的方向、反射角」。然而，单就「反射面」这一个概念而言，我们就无法见到「所有可被归为"反射面"的现象」，正如我们不可能见到考察「光的反射定律」的所有高考题一样，那要如何把握「反射面的外延」呢？

> [!success] 回答：依靠判别模型
>
> 其实应对办法我们早就用过，那就是建构「判别模型」，通过「判别一个对象能否被归为这个概念」，进而把握「这个概念的外延」。
>
> 在这里，我们再次提到了「判别模型」。这是早在「适用判别」章节中就讲过、又在之后不断被提及的知识。不过，有些人一直混淆「通用判别模型」和「通用处理模型」。所以，接下来我们将用「信息推测」来描述二者，这样，大家就能轻松地区分，还能体会到「用信息推测来描述知识有多清晰」。

### 1 判别模型组成

> [!question] 提问：明确判别模型
>
> 可能有些读者已经察觉到一个潜在问题。我们要依靠「判别模型」才能明确一个知识的输入输出，然而「判别模型」也是知识，也有输入输出，那「判别模型的输入输出」又要依靠什么来明确呢？
>
> 实际上，「判别模型」的输入输出，直接就能明确，并不需要额外的模型来明确，这也是「判别模型」的独特之处。

> [!abstract] 描述：一般输入
>
> 因为「判别模型」的用途，就是判别「任意一个对象」该被归为「哪个概念」，所以「判别模型」的「输入空间」就是 {（归类前的）所有对象（现象）}。不论哪个判别模型都是如此，所以就不需要额外的东西来明确「判别模型的输入空间」了。
>
> 我们将这种输入空间称为"判别模型的一般输入空间"。在数学中，与之相似的概念是「全集」。

> [!abstract] 描述：标准输入
>
> 不过，尽管在原则上，「任何概念的判别模型」都要能处理 {所有现象}，但许多概念只会在「特定领域」中被使用。这使得这些概念的判别模型的输入空间，在实际应用中，并非 {所有现象}，而仅仅是 {特定范畴下的所有现象}，我们将这种输入空间命名为"判别模型的标准输入空间"。

> [!example] 例子：整数领域（标准输入）
>
> 例如，原则上，「偶数的判别模型的输入空间」是 {（归类前的）所有对象}，应能判别「任意一个东西」是「偶数」还是「非偶数」。诸如，判别「黄河」是「偶数」还是「非偶数」；判别「苹果」是「偶数」还是「非偶数」；判别「3」是「偶数」还是「非偶数」，等等。但由于「偶数」只在「整数」的范畴中被使用，使得「偶数的判别模型的输入空间」，在实际应用中，常常被设为 {所有整数}，有时也会被设为外延大一些的范畴，如 {所有数}。

> [!example] 例子：经济领域（标准输入）
>
> 又如，原则上，「正外部性的判别模型的输入空间」也是 {（归类前的）所有对象}，要能判别「黄河」是「正外部性」还是「非正外部性」；「接种疫苗」是「正外部性」还是「非正外部性」，等等。但由于「正外部性」是「经济学」范畴下的概念，在实际应用中，其「判别模型的输入空间」，常被设为 {经济活动中的所有现象}，有时也会被设为外延大一些的范畴，如 {活动中的所有现象}。

> [!abstract] 描述：对象层优势
>
> 在实际应用中，我们往往不会选择判别模型的「一般输入空间」，而是选用「标准输入空间」。其优点在于：我们可以不必每次都带着「所有的具象对象」去思考，可以节省建构判别模型时所需的经验。
>
> 课程在「抽象层级」章节中引入过「对象层」的概念。其实「判别模型的标准输入空间」就符合「对象层」的内涵，因为它不是「所有具象对象所组成的集合」，而是「我们当前关注的对象所组成的集合」，限定了一个范围，把不常见的对象排除了。在数学中，与「对象层」相对的概念是「论域」。
>
> 不过，尽管我们在实际应用中可以选用「特定的对象层」作为「输入空间」，来排除一些不常见的对象，但这并不改变「判别模型的输入空间」在原则上是 {（归类前的）所有对象}。如果「用特定对象层作为输入空间所建构的判别模型」遇到了「所选对象层之外的对象」，就还需要组合其它判别模型一起来判别了。

> [!example] 例子：经济对象层（对象层优势）
>
> 例如，在建构「正外部性的判别模型」时，我们可以将它的输入空间设定为 {经济活动中的所有现象}，这样就不必带着「苹果」「偶数」「黄河」「给压岁钱」等对象一起思考了。不过，我们仍要能判别「给压岁钱」这种对象属于「正外部性」还是「非正外部性」。这时就要组合「经济活动的判别模型」来一起判别：由于「给压岁钱」不属于「经济活动中的现象」，所以即使它满足「导致其他主体获利，但获利者无须付出代价」的属性，也不属于「正外部性」。

> [!abstract] 描述：一般输出
>
> 「判别模型的输出空间」同样不需要额外的模型来明确，因为「一个概念的判别模型的输出」无非就是「此概念」或「非此概念」两个可能结果中的一个，那么这个判别模型的输出空间就是 {此概念，非此概念} 两个可能类别所组成的集合。

> [!example] 例子：偶数输出（一般输出）
>
> 例如，「偶数的判别模型的输出空间」，就是 {偶数，非偶数}这两个标签所组成的集合。

> [!example] 例子：正外部性输出（一般输出）
>
> 又如，「正外部性的判别模型的输出空间」，就是 {正外部性，非正外部性}这两个标签所组成的集合。

> [!abstract] 描述：判别输入输出
>
> 可见，对于任何一个概念，我们都能直接写出其「判别模型的输入输出」：
>
> * 输入空间：{特定范畴下的所有对象}或 {（归类前的）所有对象}
>
> * 输出空间：{此概念，非此概念}
>
> * 映射关系：
>
> * 如果「某对象」具备「此概念的内涵」，则归为「此概念」。
>
> * 如果「某对象」不具备「此概念的内涵」，则归为「非此概念」。

### 2 判别模型特性

> [!abstract] 描述：判别模型用途
>
> 什么时候会用到判别模型呢？
>
> 当我们需要把「较具象的对象（输入）」归类到「较抽象的概念（输出）」，进而选择「这个对象的应对方式」时，就会使用到「判别模型」。

> [!example] 例子：苏格拉底是人（判别模型用途）
>
> 例如，当我们想要处理「苏格拉底」时，可以用「人（身份）的判别模型」将其判别到「人（身份）」的概念下，再用"「人」是「会死的」"来推出"苏格拉底（具体的人）是会死的"。

> [!abstract] 描述：输出为标签
>
> 这种用途使得判别模型具有两大特点。
>
> 其一，「判别模型的输出」实质上是一种"标签"，而非「带有属性的概念」，只能告诉我们「一个输入对象」应该被归为「哪个概念」下。
>
> 当我们想要从「判别模型」获得「输出概念的属性」时，其实是将「判别模型所用的共有属性（内涵）」作为「输出概念的属性」，但「共有属性（内涵）」并不是「判别模型的输出」，而是「判别模型的判别依据」。
>
> 相比之下，「判别模型的输入」是「带有属性的对象」，因为「判别模型」需要将「对象的属性」与「概念的内涵」进行比较，以确定「这个对象」应该归为「哪个概念」。

> [!example] 例子：偶数标签（输出为标签）
>
> 例如，当「偶数」作为「判别模型的输入」时，是带有属性的，但当「偶数」作为「判别模型的输出」时，则仅为一种「偶数的标签」。

> [!warning] 类比：文件夹标签（输出为标签）
>
> 若用文件夹做类比，当「文件夹A」被用于「判别模型的输入」时，会带有「文件夹A的属性」，如「创建时间」「文件夹大小」「文件数量」等。但「文件夹A」作为「判别模型的输出」时，则仅为一种分类标签，这个标签只能告诉我们「某文件」该归于「文件夹A」还是「非文件夹A」，并不会同时输出「文件夹A」属性。

> [!abstract] 描述：从属特点
>
> 其二，由于「判别模型」把「输入空间（对象层）的所有现象」分成了两类，分别对应着「输出空间的那两个概念（类别）」，所以「判别模型」的「输入空间（对象层）的任何一个对象」一定可以被归到「输出空间中的某个概念（类别）」下。

> [!example] 例子：2从属偶数（从属特点）
>
> 例如，对于「偶数的判别模型」，其输入空间是{给压岁钱，3，2，苹果，...}（所有具体的现象），其中"给压岁钱"可以被归到「非偶数」这个类别下，"2"可以被归到「偶数」这个类别下。所有输入常量，要么可被归为「偶数」，要么可被归为「非偶数」。

> [!abstract] 描述：判别能力
>
> 接着，我们再来看看判别模型所具备的能力，它能做什么，不能做什么。
>
> 其实，「判别模型」并不能直接得到「完整的外延」，它只能在遇到「一个对象」时，判别「这个对象」属于「哪个概念的外延」。我们将这种能力称为"判别能力"。
>
> 「判别能力」不同于「具象能力」，并不能"由「概念（标签）」联想「可被归为此概念的所有对象」"。虽然理论上我们可以把「所有对象」都给「判别模型」，再根据判别结果，构造出「概念的完整外延」，但在实际中，我们不可能收集到「所有对象」，以至于「判别能力」并不能用于凭空罗列「概念的完整外延」。

> [!example] 例子：难举例子（判别能力）
>
> 正因如此，我们可以观察到一种现象：某些学霸在遇到具体问题时，能判断这个问题，该用什么概念的相关公式来解决。但若反过来问，「当前这个概念的相关公式」可以用来解决「哪些具体问题」时，他们往往很难想出太多例子。这正是因为，解决具体问题时，运用的是「判别能力」，而举例子时，需要的是「具象能力」。

> [!quote] 过渡：引出联结
>
> 介绍完判别模型的输入输出、特点、能力后，接下来，再来看看"处理模型"的输入输出。
